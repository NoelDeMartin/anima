{
  "scripts": {
    "build": "pnpm -r build",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "oxfmt": "^0.33.0",
    "oxlint": "^1.48.0"
  },
  "packageManager": "pnpm@10.30.0"
}
