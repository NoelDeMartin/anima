{
  "name": "@anima/native",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tauri build",
    "build:debug": "cargo build --manifest-path src-tauri/Cargo.toml",
    "dev": "tauri dev",
    "tauri:prepare": "bash scripts/prepare.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
